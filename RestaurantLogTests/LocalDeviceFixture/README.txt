This folder may contain a local copy of an on-device Core Data store for the
legacy-consolidation integration test. All database, WAL, and external-blob
files are git-ignored. Only this explanation is committed.
